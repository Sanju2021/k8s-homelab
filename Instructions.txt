Instructions:


🚀 First steps after downloading

Unzip and git init in the folder, then push to GitHub
Enable WinRM on your Dell server (commands in docs/terraform.md)
Copy terraform.tfvars.example → terraform.tfvars and fill in your details
Generate an SSH key: ssh-keygen -t ed25519 -f ~/.ssh/k8s-homelab
Run ./scripts/setup.sh — it handles everything end to end